<h1>RENTOPETTO</h1>
<p>Find your Purfect companion.</p> %>

<div class="wrapper">
  <div class="center">
    <% if user_signed_in? %>

      <div class="center">
        <div class="logo">
          <img src="<%= asset_path( 'logomove.gif' ) %>" />
        </div>
      </div>


         </div>
      </div>
    <% end %>
  </div>
</div>
