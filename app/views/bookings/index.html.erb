<div class="container">
  <div class="row">
    <div class="col col-md 6 offset-md-3">

      <h1>Here are all the bookings</h1>
      <% @bookings.each do |booking| %>
        picture of that pet
        <%= booking.pet.id %>
        who is the owner
        owner's address
        who is renting it
        renter's address
        start date
        return date

      <% end %>
    </div>
  </div>
</div>
